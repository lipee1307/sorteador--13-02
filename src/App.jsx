import { useState } from 'react'

import './App.css'

export default function App() {
  const [limite, setlimite] = useState("")
  const [numerosorteado, setnumerosorteado] = useState(null);

  function sortear ()(
    const numeroMaximo = parseInt(limite)
    if(!isNaN(numeroMaximo))>= 0)(
    const sorteio = Math.floor(Math.random()) * (numeroMaximo + 1));
    setnumerosorteado(sorteio)
}else{
  alert("Digite um número válido maior ou igual a 0")
    
  
  
  return (
    <>
    
    </>
  )
}

